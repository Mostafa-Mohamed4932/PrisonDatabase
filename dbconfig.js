   // config for your database
const config = {
    user: 'sa',
    password: '123456',
    server: 'localhost', 
    database: 'PrisonDatabase',
    trustServerCertificate: true
};

module.exports = config;